embed_dim: 512
topk: 4
agg_type: "bi-interaction"
pool: "attn"
dropout: 0.25
act: "leaky_relu"
moe_args: {
  num_experts: 30,
  num_slots: 10,
  num_heads: 16,
  slot_dim: 256,
  share_lora_weights: True,
  dropout: 0.1,
  auto_rank: True,
  keep_slots: True,
}
