embed_dim: 512
n_fc_layers: 1
dropout: 0.25
gate: True
attention_dim: 384
k_sample: 8
subtyping: False
instance_loss_fn: 'svm'
bag_weight: 0.7
moe_args: {
  num_experts: 30,
  num_slots: 10,
  num_heads: 16,
  slot_dim: 256,
  share_lora_weights: True,
  dropout: 0.1,
  auto_rank: True,
  keep_slots: True,
}
