embed_dim: 512
num_fc_layers: 1
dropout: 0.25
num_attention_layers: 2
num_heads: 4
moe_args: {
  num_experts: 30,
  num_slots: 10,
  num_heads: 16,
  slot_dim: 256,
  share_lora_weights: True,
  dropout: 0.1,
  auto_rank: True,
  keep_slots: True,
}
